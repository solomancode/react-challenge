import styled from "@emotion/styled";

export const EmployeeFormStyle = styled.form`
    background-color: #eee;
    padding: 15px;
    border-radius: 3px;
    display: flex;
    align-items: flex-end;
    gap: 10px;

    .submit {
        height: 32px;
        padding: 0 1em;
    }
`